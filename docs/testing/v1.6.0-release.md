# v1.6.0 发布验证

2026-09-23 发布 [Easy Trip v1.6.0](https://github.com/chengweiv5/easy-trip/releases/tag/v1.6.0)，已设为 Latest 正式版。新增湖畔晴空、松林晨光、落日陶土、山岚暮紫和玫瑰沙丘五套浅色主题；首页调色盘与工作台更多菜单均可进入。选色即时预览，应用后全局保存，返回可放弃，重启恢复已选主题。主题页已移除虚构旅行预览卡片。

## 发布产物

- 标签 `v1.6.0` 与 APK 内嵌源码提交均为 `a7eddbfbed9afde107f00152403f086b70817e18`；`SOURCE_STATE=CLEAN`、`DEBUG=false`、`BUILD_TYPE=release`。
- 包名 `com.yangchengwei.easytrip`，版本 `1.6.0`，versionCode `8`，最低 Android 8.0。
- [正式 APK](https://github.com/chengweiv5/easy-trip/releases/download/v1.6.0/easy-trip-v1.6.0-release.apk)，62,225,005 字节；[SHA256 文件](https://github.com/chengweiv5/easy-trip/releases/download/v1.6.0/easy-trip-v1.6.0-release.apk.sha256)。
- APK SHA256：`f65d055d91a982f4bb2c365db3880e63dab84e3fe74f202fbbd03aa22e6a264e`。
- 签名证书 SHA256：`a12b60ce83aaacd76899a66d81d78f3fc4cfe3b7424f9a5cf7479732c415048e`，沿用既有正式版签名。签名与 16 KB zipalign 检查通过，高德配置一致，APK 不含私有签名配置或密钥文件。
- 发布的是此前已安装并回读校验的华为真机 APK，本次未重新构建或替换它。发布源码已快进推送到 `origin/main`，远端标签回读一致；后续文档提交不改变标签及 APK。

## 验证结果

- 在源码提交 `a7eddbf` 的干净检出重新执行 `:app:testReleaseUnitTest :app:lintRelease` 成功；850 项单元测试通过，0 失败、0 错误、0 跳过；lint 0 Error/Fatal、48 Warning、1 Hint。
- 主题功能实现阶段的 8 项主题验收、5 项日历对齐验收，以及五主题地点池/行程页共 10 个真实高德地图画面检查通过。地图中心点、缩放和实例保持；日期路线色、错误语义色和分享长图配色保持原规则。
- 移除虚构卡片后的 `ThemePickerTest` 5/5 通过，五主题与小屏大字体截图经视觉和 OCR 检查。此前扩展回归 65 项中 62 项通过，3 个失败在功能修改前基线 `c1663fb` 完全复现；本次不宣称扩展回归全部通过。
- 独立 Android 36 模拟器先安装并校验官方 v1.5.0，再以最终 v1.6.0 APK 覆盖升级。7 张业务表逐行、逐值一致：`trips=2`、`trip_days=6`、`saved_places=6`、`tags=2`、`saved_place_tags=2`、`itinerary_items=18`、`route_legs=12`。日期、时间、停留时长、备注、交通及标签关系完整保留；UID、首次安装时间和数据目录未变。这些是专用测试样例。
- 升级后首页和五主题选择页正常打开，无虚构预览，默认湖畔晴空保持，检查的 AndroidRuntime 日志无崩溃。
- 此前同一 APK 已覆盖安装到华为 ALN-AL00，设备回读包哈希一致；原有 2 个旅行、首页 31 个地点/9 天摘要及山岚暮紫主题保留，UID、首次安装时间和数据目录未变。此项验证用户可见信息，未逐行读取手机数据库；本次发布没有再次操作手机。
- GitHub 两项资产大小与摘要比对通过。正式发布后从公开地址重新下载 APK 和校验文件，两者与本地产物逐字节一致；下载包签名、版本和非调试属性复验通过。原生 API 回读 `draft=false`、`prerelease=false`，Latest 为 v1.6.0。

## 证据与回滚

本地证据位于 `.scratch/release-v1.6.0/evidence/`：`artifact.json`、`release-checks.log`、`upgrade-verification.json`、`upgrade-review.jpeg`、`public-download-verification.json`、`latest-after.json`、`release-refs.txt`。真机原始记录位于 `.scratch/v1.6.0-theme-preview-removal/device-verification.json`。README 保留的工作台图片来自 `46af47e`，未将旧截图标为本版主题截图。

源码回滚通过新的 revert 提交按快进规则交付；设备修复包应递增 versionCode 并沿用相同签名。不要卸载或清除已升级设备的数据，不覆盖现有 v1.6.0 标签和发布资产。文档修改前备份在 `.scratch/release-v1.6.0/before/`，可通过反向提交恢复本次文档变更。
