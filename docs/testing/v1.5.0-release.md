# v1.5.0 发布验证

2026-09-23 发布 [Easy Trip v1.5.0](https://github.com/chengweiv5/easy-trip/releases/tag/v1.5.0)，已设为 Latest，包含工作台日期操作、地图视角及全程日历对齐修复，以及最新 README 功能截图。

## 发布产物

- 标签 `v1.5.0`、APK 内嵌源码提交均为 `a37877a50293917007ac6e9150276a896d895ae1`；从干净工作区构建，`SOURCE_STATE=CLEAN`、`DEBUG=false`、`BUILD_TYPE=release`。
- 包名 `com.yangchengwei.easytrip`，版本 `1.5.0`，versionCode `7`，最低 Android 8.0。
- [正式 APK](https://github.com/chengweiv5/easy-trip/releases/download/v1.5.0/easy-trip-v1.5.0-release.apk)，62,192,237 字节；[SHA256 文件](https://github.com/chengweiv5/easy-trip/releases/download/v1.5.0/easy-trip-v1.5.0-release.apk.sha256)。
- APK SHA256：`06a353f16f35331850d7da3c3c597e42b9cea9c74ff7b91f55a11061bb5ace2d`。
- 签名证书 SHA256：`a12b60ce83aaacd76899a66d81d78f3fc4cfe3b7424f9a5cf7479732c415048e`，与官方 v1.4.0 一致。签名验证、16 KB zipalign 检查通过，APK 未包含私有签名配置或密钥文件。
- 已快进推送发布提交至 `origin/main`，回读 main 和远端标签指向；发布后的文档补充提交不改变发布标签和 APK。

## 验证结果

- `:app:assembleRelease :app:testReleaseUnitTest :app:lintRelease` 成功；845 项 Release 单元测试通过，0 失败、0 错误、0 跳过；lint 0 Error、48 Warning、1 Hint。
- 日历代码提交 `46af47e` 已通过 7 项模拟器界面检查：5 项页头回归、30 天翻页/横滑、生产导航截图。分页与日历列边界均为 x=387..1122px，分页和两列标题、日期中心误差均为 0px。发布前未再改动这些应用源码。
- 在独立 API 36 模拟器安装已下载并校验的官方 v1.4.0，再用本次最终 APK 覆盖升级；2 个旅行、6 个旅行日、3 个地点、9 条行程安排的测试数据逐表逐行一致，其余 3 张空表保持为空。此数据为专用测试样例，不是用户手机数据。
- 升级后首页、地点池、行程和全程日历正常打开，日期、到达时间、停留时长及备注保留；全程日历页头对齐经过正式 APK 截图检查，未发现崩溃日志。
- 上传时比对 GitHub 返回的两个资产 SHA256 和字节数；发布后再次通过 `gh release download` 下载 APK 与校验文件，两者均与本地发布产物逐字节一致，签名和版本再验通过。回读确认 `draft=false`、`prerelease=false`，Latest 为 v1.5.0。
- 本次未安装到物理手机，模拟器检查不代表所有机型验收。

## 证据与回滚

本地原始日志、产物和回读记录在 `.scratch/release-v1.5.0/`：`evidence/checks.json`、`artifact.json`、`upgrade-verification.json`、`public-download-verification.json`、`release-refs.txt` 及日历截图。

源码回滚应通过新的 revert 提交按快进规则交付；修复发布需递增 versionCode，沿用相同签名。不要通过卸载或清除数据回退已升级设备，也不要覆盖现有 v1.5.0 标签或发布资产。
