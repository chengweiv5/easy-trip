# v1.4.0 行程长图分享验证

用户已确认 R3 设计及交通冲突规则。当前实现包含头部全程日历、每日地图、全程／单日、备注开关、长图放大、相册保存与 Android 系统分享。

## 使用

旅行工作台 → 更多 → 分享行程长图。默认全程且包含备注；「选一天」确认日期后切换范围。所有输出使用与预览相同的 PNG（1080px 宽）。系统分享只打开目标选择面板，发送由用户完成。

- 地点与交通从只读快照导出，保留重复到访；只使用同日当前相邻节点的路线。
- 摘要后输出日历，三天一组向下续排，所有组共用小时刻度。停留按每日配色，交通浅灰；有效交通时长优先采用人工覆盖，再采用成功路线结果，不把空隙填作赶路。
- 未设到达时间进入待安排；未知停留仅在源日虚线占位，不推断离开。短时段、长名称与重叠内容完整移入补充区。交通来不及时保留原安排和预计用时，标明「交通可能来不及」。
- 已知跨午夜时段沿真实旅行日切分；单日包含来自前日的延续并标明源日编号。超出所选日或旅行日期仍保留完整起止说明。关闭备注不改变日历。
- 成功且有效的路线画实线，其他已定位相邻点画虚线，注明到访顺序。每天单独取景并使用该日颜色；重叠编号合并，清单不合并。
- 无授权、缺坐标或截图超时仍保留清单并说明原因，可重新生成。有效快照缓存支持离线重用；临时缓存保存 7 天。
- 先测量完整内容再分配图片；最高 30000px，像素预算不超过 2400 万或最大堆的 1/16（以像素数计，ARGB 位图约占堆的 1/4）。超预算引导单日，单日仍超预算可关闭备注或调整内容，不静默截断。
- FileProvider 只暴露导出缓存子目录，分享使用只读 content URI。Android 10+ 用 MediaStore pending 写入，8–9 仅保存时申请存储权限。

## 已执行验证

2026-09-22，rebase 至 `origin/main` 的 `90d337a` 后验证：848 项单元测试通过，14 项分享相关仪器测试通过，lint 0 错误（49 条警告、1 条提示），debug APK 与 Android 测试 APK 构建成功。这是当前代码的结果；R2 阶段的 812 单测／12 项仪器检查保留在历史日志中。

环境：JDK 21、Android API 36，独立只读 AVD 副本 emulator-5590，软件图形渲染。首次使用宿主 GPU 的运行在高德创建图形上下文时失败（`createContext failed: 12288`）；更换本任务临时模拟器渲染后完整复测通过。未安装或操作连接真机。

- 单元测试覆盖保留重复到访、逐日编号、空日、跨日／过期路线、有效实线、损坏路线回退、交通覆盖时长和缺坐标。
- 新增 13 项日历投影单测，覆盖 7 日分组、原编号、未知时间、零时长、完整交通时长、冲突不改原数据、仅前日延续的单日、整段落在次日或旅行外的交通，以及大历史时长的安全计算。
- 新增 4 项日历仪器测试，使用 Android 实际字体测量检查长名称和密集时段文字不重叠／不越界，生成常规、7 天、冲突、单日延续、全部未定及密集样例；验证交通像素、测量高度与 PNG 一致、预算超限在写文件前拒绝。
- 仪器测试实际生成 PNG，检查尺寸、地图像素、长备注增长、隐藏备注、单日高度、页脚与超预算拒绝。
- 读取 FileProvider URI 与 MediaStore 相册结果，字节与预览 PNG 一致，MIME 和授权正确；测试创建的相册记录完成后清理。
- UI 覆盖默认选项、单日选择、备注、连续滚动、放大返回、快速切换期间禁用旧输出、空行程、读取失败重试。
- 高德 SDK 真实截图通过，保留署名、地图编号与每日颜色；密集编号合并、取消释放、无授权／撤回授权回退通过。首次冷启动曾超时，回退不阻断清单；后续真实截图通过。
- Android 原生分享面板已检查截图与前台包，取消后返回原预览并恢复按钮。首次设备开机遗留 System UI 弹窗已按日志确认并清除，之后通过。
- 更多菜单四项可见，分享前先关闭菜单；菜单边界按新高度计算。真实 Room 数据的旅行列表 → 工作台 → 分享 → 返回导航链路通过。

本轮日志、实际 PNG、分区检查图及 OCR：`.scratch/v1.4.0-itinerary-long-image/calendar-implementation/`。已逐区检查顶部日历、交通补充、地图与页脚，未发现内容裁切。常规输出 1080×7477，7 天样例 1080×9067；图片高度随内容变化。设计评审稿：`design/v1.4.0-itinerary-share-r3.html`；可编辑源：`design/easy-trip-v1.4.0.pen` 已保存并通过同哈希磁盘副本回读确认交通节点。设计示意路线与自动化 fixture 路线不代表真实导航规划结果。

构建命令：`JAVA_HOME=/Library/Java/JavaVirtualMachines/jdk-21.jdk/Contents/Home ./gradlew :app:testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleDebugAndroidTest --console=plain`。仪器测试使用 `adb -s emulator-5590 shell am instrument -w -r -e package com.yangchengwei.easytrip.share com.yangchengwei.easytrip.test/androidx.test.runner.AndroidJUnitRunner`。

## 限制与回滚

Android 8–9 的运行时存储授权、各品牌相册、真实聊天客户端压缩与接收效果尚未真机验证。地图首次加载依赖高德可用性；最长每个非空日等待 12 秒，失败显示回退说明。不含账号、公开链接或多人编辑，不发布正式 Release。

无数据库迁移。回滚应用改动可在功能分支对本次功能提交执行 `git revert <commit>`，检查后再按项目快进规则交付；不回退用户已保存到相册的图片。重要原文件备份位于本地 `implementation-before/`。
