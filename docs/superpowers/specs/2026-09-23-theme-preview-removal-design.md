# 主题页移除虚构旅行预览

用户在真机验收后要求主题配色效果预览中不要出现虚构旅行卡片。

采用最小修订：移除整块虚构行程和其“效果预览”标题，主题选择列表直接置于页面内容顶部。页面背景、文字、选中行和应用按钮继续随临时主题变化，承担配色预览；仅点击应用才保存，返回仍放弃。

不引入真实旅行数据依赖，不新增示例色块区，不改变主题列表、持久化或导航逻辑。五套 Pencil 选择页同步移除两块节点，保持 62dp 行高及底部固定操作。

验证使用现有 ThemePickerTest 的五套主题切换、保存失败与小屏/大字用例，构建通过后检查实际截图与可编辑设计节点。
